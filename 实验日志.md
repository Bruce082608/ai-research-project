# 实验日志

> 每个实验块结束后追加一条。数据只取自 `03_实验/results/*.json`；凭记忆填写的数字不允许出现。
> 条目模板见提示词附录。尚无实验块。

## 环境基线（2026-09-03，工具返回）
- 主机：Windows 10（win32 10.0.19045），PowerShell
- Python 3.12.10；git 2.55.0.windows.3
- GPU：无 NVIDIA GPU（`nvidia-smi` 不存在）
- 已安装科学计算 / LLM 相关包：仅 `requests 2.34.2`；`numpy / pandas / matplotlib / scipy / torch / transformers / datasets` 均缺失
- 含义：本机只能承担 CPU 级轻量计算与绘图（安装 numpy/matplotlib 后）；模型训练或大规模推理需依赖 API 预算或用户调度的远程 GPU（见 `QUESTIONS.md` Q-002）

## 远程 GPU 环境基线（2026-09-03，用户租用 AutoDL 实例，工具返回）
- 连接：本地 `~/.ssh/config` 别名 `autodl`（`connect.nma1.seetacloud.com:39679`，root，ed25519 公钥免密）；辅助脚本见 `03_实验/remote/`
- 硬件：**1× NVIDIA A800 80GB PCIe**（81920 MiB，驱动 580.105.08）；14 vCPU；内存 1007 GB（宿主机共享）
- 系统盘 `/` 30 GB；数据盘 `/root/autodl-tmp` 50 GB（代码、模型、缓存、结果均放数据盘）
- 软件：Ubuntu 22.04.5；miniconda Python 3.12.3；torch 2.8.0+cu128（CUDA 12.8，bf16 支持）；transformers 5.16.1、datasets 5.0.1、accelerate 1.14.0、peft 0.20.0、huggingface_hub 1.30.0、pandas 3.0.5、scipy 1.18.1；tmux 3.2a、git、rsync；无 nvcc
- 网络：阿里云 / 清华 pypi 镜像对该机房返回 403，已切换为腾讯云镜像；HuggingFace 走 `hf-mirror.com`（`HF_HOME=/root/autodl-tmp/hf_cache`）；**GitHub 不可达**，代码通过 `sync.ps1 push`（scp）上传；AutoDL 学术加速在该地区不可用
- 冒烟：bf16 8192² 矩阵乘 102 TFLOPS；`Qwen/Qwen2.5-0.5B` 经镜像下载 72 s、greedy 生成 32 token 0.92 s（34.8 tok/s）、峰值显存 1.01 GB、输出 "Paris. ..." 正确 → `03_实验/results/smoke_llm.json`（tmux 后台方式复跑一致，日志 `03_实验/logs/smoke_tmux_0903_2044.log`）
- 计费：按量计费，用户预计使用约 100 GPU·h；关机停 GPU 计费但保留数据盘；长任务用 `run_bg.sh <name> --shutdown -- <cmd>` 跑完自动关机
