# STATE

- 课题：**候选 3「良性微调是否侵蚀拒绝方向？机制检验与定向锚定」已写成 Research Brief v1.0，等待 G1 批准**（Q-007）。备选：候选 1（串行 vs 并行推理时计算路由）。
- 研究问题与 H0/H1（一句话）：良性 LoRA 微调是否降低有害提示在拒绝方向上的投影并导致拒绝率下降（H1-1）；训练时锚定该方向能否在效用损失 ≤1 pt 下恢复拒绝率（H1-2）；随机方向锚定无效（H1-3）。详见 `01_选题/Research_Brief.md` 第 1 节。
- 当前阶段 / 里程碑：Phase 1 结束 / M1 全部工作包完成 → **关卡 G1 待批**。
- 已通过的关卡：无。
- 上次会话结束摘要（≤5 行）：
  1. 2026-09-03 第 1–2 轮：初始化；D-001 边界条件；32 篇文献元数据核验；6 个候选（`01_选题/候选课题.md`）。
  2. 第 3 轮（另一会话）：AutoDL 1× A800-80GB 配置与冒烟，D-002（约 100 GPU·h，阈值默认 25）。
  3. 第 4 轮：D-003（选题选择并入 G1）；再核验 20 篇 → R-033～R-052；完成对抗审查（存活候选 3 主 / 候选 1 备）与候选 3 的 Research Brief；提交 G1；新增 Q-007（G1 决策）、Q-008（人工标注 100 条）。
- 进行中的工作包与步骤（勾选）：M1 全部 [x]；M2 未开始（等待 G1）。
- 下一步（1–3 条，可直接执行；G1 批准后）：
  1. 进入 M2：做 3.6 节反漂移检查；把 Research Brief 第 7 节 C-1～C-7 转为检索词与数据库清单，提交 **G2** 请求。
  2. G2 批准后：检索 + 读摘要/原文，把 R-014、R-015、R-016、R-018、R-034、R-037、R-038 等升级为「已核验」，写精读卡（先列顺序清单请用户确认）。
  3. 并行：写 `03_实验/protocol.md` 草稿骨架（不写代码），把 Brief 第 3 节展开为变量表 / 消融矩阵 / 统计方案。
- 阻塞与待决策（指向 Q-编号）：**G1 / Q-007 待答**（阻塞 M2 检索与 M3）。Q-008（人工标注意愿）M4 前需答。Q-006 阈值默认 25 GPU·h 可改。
- 算力提醒：远程实例按量计费，M1–M3 不需要 GPU，**不跑任务时请在 AutoDL 控制台关机**；开机后若端口变化需更新 `~/.ssh/config` 的 `autodl` 条目。用户在聊天中贴过实例密码，建议重置。
- 关键文件索引（路径 + 一句话说明）：
  - `fable51-长程科研任务执行提示词.md` / `fable51-科研工作台.md`：用户提供的规程与细则，不得修改。
  - `PLAN.md`：M1 全部完成；M2–M5 工作包级（v0.2 + M1 勾选）。
  - `DECISIONS.md`：D-001 边界条件；D-002 算力形态；D-003 选题选择并入 G1。
  - `QUESTIONS.md`：Q-001～Q-005 已答；Q-006 部分已答；Q-007（G1）、Q-008 待答。
  - `ERRATA.md`：E-001。
  - `实验日志.md`：本机 + 远程环境基线与冒烟结果。
  - `01_选题/候选课题.md`（6 候选 v1.0）、`01_选题/对抗审查.md`（v1.0，排序与收敛）、`01_选题/Research_Brief.md`（v1.0，候选 3，待 G1）。
  - `02_文献/引文库.md`：R-001～R-052，全部「待核验（仅元数据）」；批次 A 候选阶段、批次 B 对抗审查阶段。
  - `03_实验/remote/`：远程工具脚本（setup / check_env / run_bg / smoke_llm / sync）；`03_实验/results/smoke_llm.json`、`03_实验/logs/`：冒烟结果。
  - `03_实验/code/`、`04_论文/`：空。
- 最后更新：2026-09-03
