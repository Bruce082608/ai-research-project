# Fable 5.1 科研全流程工作台（LLM 方向 · 计算驱动版）

> 适用：Anthropic Claude Fable 5.1（AI4S 定位）。领域：大语言模型研究；形态：计算/数据驱动实验。
> 用法：**每个新会话先贴「第 0 节」启动模板**，然后按阶段复制对应提示词；所有「【】」占位符换成你的实际内容。
> 原则：Fable 负责高强度的读、写、推理、编程；你负责事实核验、算力调度、结论责任。全程人类在环。

---

## 0. 会话启动模板（每次新建对话第一句）

```
你现在是我的科研工作台，处理【大语言模型】方向的课题【填入课题一句话】。
工作守则：
1. 不编造：任何文献、数据、数值、基准分都必须来自我提供的内容或真实工具检索结果，来源不明的一律标注「待核验」；
2. 引用纪律：只允许引用我上传/你已真实读到的文献；需要更多文献时，先列出检索词与数据库（arXiv/ACL Anthology/Semantic Scholar API），经我同意再检索；
3. 输出纪律：禁止复述你的内部推理链（反蒸馏约束）；给结论时必须给可核验的结构化论证；
4. 不确定就直说「不确定」，并给出如何验证的建议，禁止自信地瞎猜；
5. 每个阶段交付前先给我 3 行总结：已完成 / 关键结论 / 需要我决策的点。
请先复述一遍上述规则，然后等待我的第一项任务。
```

---

## 1. 选题与构思

### 1.1 候选方向生成（LLM 专用）

```
我在大语言模型方向做计算型研究，算力预算约为【例如 8×H100×2 周 / 纯 API 预算 $3000】，偏好投稿【ACL/EMNLP/NeurIPS/ICLR，可多选】。
请基于 2026 年前后的前沿现状，给出 6 个候选课题，覆盖以下桶中的至少 3 个：
- 训练/后训练效率（数据合成、蒸馏、稀疏化）
- 推理时计算（scaling、搜索、验证器）
- 对齐与安全（RL 变体、可解释性归因）
- 评估方法论（基准偏差、长上下文/Agent 评测）
- 长上下文 / 检索 / Agent / 多模态任选其一
每个候选必须包含：
a) 要回答的 1 个核心研究问题（可证伪形式）；
b) 新颖性来源：与你所知最相关 2–3 篇近作的具体差异；
c) 最小可行实验（数据集、模型规模、指标、预计算力）；
d) 失败风险与「如果结论为负是否仍有发表价值」。
先不要推荐，先给出结构完整的 6 个。
```

### 1.2 对抗审查（换人设，逐轮收紧）

```
现在你切换为该方向最严苛的 3 位审稿人：一位统计方法专家、一位系统工程实践者、一位追求 novelty 的理论家。
逐一攻击上面 6 个候选：指出每个最致命的 1–2 个缺陷（例如：基准已被刷爆、baseline 不公平、负结果无意义、算力不可行、与某篇 2025 年工作实质等价）。
攻击完，按「存活概率 × 影响力 × 算力可行性」给你自己的排序，并诚实标注哪些攻击其实站不住脚。
```

### 1.3 选题收敛 + Research Brief 模板

收敛标准：一轮对抗后仍然存活的课题 ≤ 2 个时，进入定型：

```
请将我们最终选定的课题【课题名】写成一份 Research Brief，包含：
1. 研究问题与可证伪假设（H0/H1 形式）；
2. 与最相关 3 篇文献的差异表（维度 × 对比）；
3. 最小可行实验设计（MVE）与完整版实验设计（full）的分层；
4. 主要风险与提前止损点；
5. 一句话 elevator pitch（给导师/合作者看）。
任何一句声称「SOTA 尚未解决」的论断，都必须列出你需要核验的文献来源。
```

**出口标准**：假设、差异点、MVE 三者你能用自己的话向同行复述，才进入第 2 节。

---

## 2. 文献调研与综述

### 2.1 Grounding 规则（贴在任何文献任务前）

```
本次对话只允许引用我上传的 PDF/文本与工具真实检索到的文献。
你的每一个引用必须带可定位证据（arXiv 编号 / 原文页码 / 直接摘录句）。
写作中任何「据 X 等人（2025）」的说法，先把该文献的摘录贴给我确认，再入文。
若你记忆中有一篇重要工作但我没给，先描述「我记得有一篇关于……的工作」，不要自行编造作者与年份。
```

### 2.2 单篇精读卡片模板

```
请按下列卡片结构精读我上传的文献，不要发挥：
- 核心问题与主张
- 方法（一句话机制 + 关键组件）
- 数据与实验设置（规模、baseline、指标）
- 证据强度评价（消融是否充分、统计是否可靠、有无 cherry-picking）
- 局限与作者未回答的问题
- 与我课题【课题名】的关联：直接相关 / 间接背景 / 仅方法论可借鉴
一次处理一篇；多篇时先列清单让我确认顺序。
```

### 2.3 批量对比矩阵

```
把已精读的 N 篇按列汇总成一张 Markdown 对比表：
列 = 方法 / 核心机制 / 训练数据与规模 / 评测基准与指标 / 相对增益 / 关键局限 / 与我的课题差异。
完成后指出：哪 2 篇之间的对比最常被忽视？哪 1 个空白点最像可切入的 gap？
```

### 2.4 综述写作（先引文清单，后成文）

```
我将撰写综述/related work 部分，主题为【主题】。
第一步：只输出大纲（按论证主线分段），并列出每段将支撑的引文清单（只允许已核验的文献）；
第二步：等我对大纲和引文清单确认后，再逐段成文，每段结尾附支撑引文编号；
第三步：成文后输出一份「引用-论断」对照表，供我校验。
```

### 2.5 幻觉自查清单（每次综述交付后跑一遍）

```
请对刚才的综述做一次自我核查：列出你引用的全部文献编号，
标记每一条是「我上传的原文可见」还是「依据记忆」；
任何记忆型引用若我无法提供原文，请在文中降级为「待核验」或删除。
```

**出口标准**：全部引用均有原文出处可回查；gap 论断有 ≥2 篇近作对照支撑。

---

## 3. 实验设计与执行

### 3.1 实验设计模板（LLM 计算实验）

```
为课题【课题名】设计计算实验。请输出：
1. 变量表：自变量（方法/超参维度）、因变量（主指标 + 稳健性指标）、控制变量；
2. 消融设计：每个组件保留/去除的对照矩阵，说明每格回答什么问题；
3. 模型与数据清单：基座模型、数据集与划分、每项的大致 token/算力成本；
4. 统计方案：重复次数、随机种子数、多重比较时是否用校正或 bootstrap 置信区间；
5. 失败判据：什么结果下该子实验判定为无效或方向不成立；
6. 计算预算表：总预算【例如 8×H100×2 周】下各实验的分配与降级路径。
先输出设计，不要写代码；我将审查后批准。
```

### 3.2 Protocol 审查指令（批准前必跑）

```
扮演方法论审稿人审查上面的实验设计，专挑这些漏洞：
- 选择偏差（挑数据/挑种子/挑 checkpoint 报告）；
- baseline 不公平（同预算、同 token、同训练步数比较？）；
- 指标被 hack 的可能（benchmark 饱和、metric 与目标不对齐）；
- 结论与实验粒度不匹配（小模型结论外推到大规模是否过度）。
输出「问题清单 + 修改建议 + 严重度」，修改后我们再定稿。
```

### 3.3 可复现实现要求（进入写代码前）

```
请按以下要求实现实验代码：
- 单一配置入口（YAML/JSON config 或 argparse），所有实验参数显式化；
- 固定随机种子清单，记录每处 RNG（torch/numpy/dataset shuffle/dataloader）；
- 训练与评测解耦，评测脚本独立可重跑，输出为结构化 JSON；
- 显式记录：模型版本、tokenizer 版本、依赖版本（requirements 带哈希更佳）；
- 先写最小冒烟测试（1 个 batch 能跑通、指标可复现），再上全量；
- 告诉我每步预计运行时长，便于我安排算力。
写完先自查一遍代码再交付，并说明任何你无法保证的部分。
```

### 3.4 执行与数据分析工作流

- **执行**：脚本由你审查后分批后台运行；每批结束让 Fable 读日志总结「通过/异常/资源瓶颈」。
- **分析**：数据只取自实验 JSON 记录，禁止让模型凭记忆填写数字：

```
请基于我提供的 results/*.json 做分析，不要使用记忆中的任何数值：
1. 汇总主指标表（均值 ± 置信区间，注明种子数）；
2. 按消融矩阵给出效应量，判断哪些差异统计上可信；
3. 画图：趋势用折线、对比用柱状/分组柱，标注误差棒与样本量；
4. 输出一段「结果解读草稿」，其中把「数据支持的结论」与「推测」分开标注。
```

### 3.5 图表规范（防止误导性可视化）

```
绘图要求：坐标轴起止不许裁剪以放大差异；误差棒必须注明为 SD/SE/95%CI 之一；
对比组的算力/token 预算若不等必须在图注写明；图例与字体达到【目标会议】投稿规范。
交付每张图时附：数据来源文件 + 生成脚本，方便复核。
```

### 3.6 实验记录模板（研究日志，防"忘了怎么跑出来的"）

每次完成一个实验块，追加到 `实验日志.md`：

```
## [日期] 实验块【编号】— 目的
- 配置指纹：git commit / config 文件路径 / 种子清单
- 实际消耗：算力 GPU·小时、token 量
- 结果摘要：关键数字 + 图/表文件路径
- 异常与处理：中途失败、重启、任何 deviating 操作
- Fable 结论 vs 我的判断：不一致点记录
```

**出口标准**：主结果可在 1 台机器上用同一 commit + seeds 复现；所有图有源数据。

---

## 4. 论文初稿撰写

### 4.1 目标定位与大纲

```
目标投稿【ACL 2027 / NeurIPS 2026，任选】，正文上限【8 页】。
基于我的 Research Brief、文献综述与实验结论，先输出论文大纲：
每个 section 的子结构 + 每段意图 + 将放置的图/表编号 + 支撑引文。
先不要写正文。
```

### 4.2 分节写作指令（一次一节，数据绑定）

- **Methods**：`基于已批准的实验设计改写为论文语言，含所有可复现细节；不新增设计内容。`
- **Results**：`只依据实验日志与 results/*.json 的真实输出撰写；每个数字必须能追溯到具体文件；图表编号与 3.5 节交付物一一对应。`
- **Introduction**：`用综述第 4 步的引文清单支撑；从大问题收窄到本文问题，最后一段给出贡献清单（3–4 条，可证伪、可验证）。`
- **Related Work**：`以「差异点」为组织逻辑（对比矩阵转写），突出与最相关工作的实质区别，不写成流水账。`
- **Discussion**：见 4.3。

### 4.3 Discussion 替代解释练习

```
针对核心结果，先给出 3 个可能的替代解释（混淆因素、指标偏差、机制性替代假设），
逐一论证为什么你的解释更合理，或诚实承认哪些无法排除。
输出为 Discussion 草稿，并把「我们无法排除的威胁」单列成 Limitation 段。
```

### 4.4 审稿人模拟（投稿前至少一轮）

```
扮演 3 位立场不同的审稿人（理论/系统/实证）审阅当前完整初稿：
1. 每人给出：主要优点、致命问题 Top 3、需补实验清单、评分倾向；
2. 汇总哪些批评是「必须回应否则被拒」级别；
3. 针对每条，给出正文/附录的修改建议或 rebuttal 措辞。
修改完成后，再快速复查一遍我给的修改是否真的回应了原批评。
```

### 4.5 Cover Letter 模板

```
基于论文写一封 cover letter（300 词内）：
- 第一段：一句话贡献 + 为什么适合【会议/期刊】；
- 第二段：与最相关工作最实质的 1 个差异；
- 第三段：可能的审稿人担忧与你的预防性说明。
语气克制、不夸大，禁止出现「首次/最好/全面超越」类未经验证的表述。
```

### 4.6 AI 使用披露（投稿前必办）

```
请根据【目标会议】最新作者政策，起草一段 AI 辅助声明（放 acknowledgements 或按会议要求），
如实说明：哪些阶段使用了大模型辅助（文献整理、实验代码、写作润色、审稿模拟），
AI 未参与实验决策与最终内容裁决，未被列为作者。
同时提示我校验该会议今年的政策原文，不要凭旧版政策作答。
```

**出口标准**：全文数字与实验记录一一对应；引用全部可回查；AI 声明符合目标会议当期政策。

---

## 5. 通用守卫与快速自查（任何阶段怀疑时使用）

```
现在做一次质量自查，只回答事实性问题：
1. 刚才的回答中，哪些内容来自你记忆而非我提供/工具检索？逐条列出；
2. 哪些数字可能记错？标注需我复核；
3. 哪些是推测而非数据支持？明确标为推测；
4. 若要验证第 1–3 条，分别建议最省力的核验方式。
```

---

## 6. 项目状态跟踪表（每阶段结束时更新）

| 阶段 | 状态 | 关键交付物 | 出口标准是否满足 | 待办/风险 |
|---|---|---|---|---|
| 1 选题 | ☐ | Research Brief | ☐ |  |
| 2 文献 | ☐ | 对比矩阵 + 引文清单 | ☐ |  |
| 3 实验 | ☐ | protocol + 代码 + 实验日志 | ☐ |  |
| 4 写作 | ☐ | 初稿 + 图表 + AI 声明 | ☐ |  |

> 复制本表到项目 README 的顶部，每阶段收尾时更新一次。四格全 ☐ 即完成一轮「选题 → 论文初稿」闭环。
