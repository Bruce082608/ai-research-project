# Protocol 方法论审查

> 版本：v0.1，2026-09-08。审查对象：`03_实验/protocol.md` v0.1-draft。
> 审查范围：选择偏差、baseline 公平性、指标可被 hack、结论粒度与统计单位。首轮依据工作区和内部算术；G2 后复核加入本批已读原文和设计核算，具体闭环状态见 §5。
> 状态：**未通过 G3；以下高严重度问题解决并复核后才能请求 G3。**

> 2026-09-08 G2 后复核：已发现直接同题工作，优先提交 G1 范围重审；下表为首轮审查历史，闭环状态以下文 §5 为准。不得把文档修订误算为实现验证通过。

## 1. 问题清单

| 编号 | 严重度 | 位置 | 问题 | 修改要求 |
|---|---|---|---|---|
| P-01 | 高 | §3 | 当前拟定分配要求 AdvBench 至少 `260 + 200 + 200 = 660` 个互斥条目；数据版本的实际条数尚未核对。若不足，复用会造成方向/锚定/评测泄漏。 | 锁定数据版本并运行样本 ID 去重检查；不足时在 `QUESTIONS.md` 建问题，未经批准不得改数据或互斥关系。 |
| P-02 | 高 | §4 | 若每个 condition/checkpoint 重新估计拒绝方向，投影变化会把方向估计变化与模型变化混在一起。 | 用不微调基座 A 一次提取并冻结 `r_hat`；层选择只用独立 dev 集，主评测标签和主结果不可见。 |
| P-03 | 高 | §2.2 | E 条件若是在 10k Alpaca 之外额外加入安全样本，会增加训练 token/更新步数，安全收益无法与 B 公平比较。 | 安全样本替换 5% 良性样本；固定总 token、batch、optimizer steps、学习率日程和 checkpoint 时间点。 |
| P-04 | 高 | §5.1/§5.3 | 主指标、标签方向、判定器版本/阈值尚未唯一确定；“有害遵从率/拒绝率”二选一会允许结果导向的口径切换。 | G3 前固定“有害遵从率”为主指标，定义标签映射、阈值、版本、生成与停止规则；拒绝率单独定义和测量，不能由 1−遵从率派生。 |
| P-05 | 高 | §4/§10 | `lambda`、锚定损失归一化和 MSE 变体仍可调；若用主有害评测选择强度会导致干预过拟合。 | 预先登记候选 `lambda`、独立 calibration/dev 集和选择规则；主评测只用于最终一次性评估，MSE 为声明过的消融。 |
| P-06 | 高 | §6 | seed、提示和 checkpoint 的层级关系尚未形成可执行的统计表；把同一提示的多个 checkpoint 当重复会夸大精度。 | 采用 prompt×seed 两层 bootstrap/混合模型；checkpoint 作为时间轨迹；运行前完成精度/功效表并固定 Holm 比较集合。 |
| P-07 | 中 | §3/§5 | 训练模板、评测模板、解码参数、scoring 版本和数据哈希尚未锁定，可能改变拒绝率而不属于研究干预。 | 在 config 中显式记录 chat template、max new tokens、temperature/top-p、stop 规则、评测器版本和输入哈希。 |
| P-08 | 中 | §2.1 | LoRA target modules、optimizer、学习率、batch/梯度累积和有效 token 数仍是拟定项；这些变量会与“方向锚定”混淆。 | G3 前锁定并在所有条件共用；仅允许 E 的 5% 样本替换这一预注册差异。 |
| P-09 | 中 | §4/§7 | R1 的层选择和 R2 的“退化至少 10 个百分点”若在看到结果后调整，会把止损规则变成事后选择。 | 在独立 dev 集上预注册阈值、置信区间和最多一次重试；主评测结果不能触发阈值改写。 |
| P-10 | 中 | §5.3 | 三项效用平均可能掩盖单任务损失。 | 同时逐任务披露；原假设是平均≤1 pt，不擅自改成每项≤1 pt。若新增单任务阈值，随范围/G3 提案批准。 |
| P-11 | 低 | §9 | 远程环境已有基线但没有锁定 requirements/config 哈希，跨运行可复现性不足。 | 实现阶段生成依赖版本清单、config 哈希、git commit、seed 清单和实际 GPU·h/token 日志。 |

## 2. 高严重度复核门槛

G3 请求前必须能在 `protocol.md`、配置草稿或数据检查记录中逐项指出：

1. AdvBench 与所有训练/dev/方向/锚定/评测集合的互斥证据及实际条数。
2. 基座 A 冻结方向、独立层选择 dev 集和不接触主评测标签的执行顺序。
3. E 与 B 的等 token、等更新步数证明。
4. 唯一主指标、判定器版本/阈值、生成设置和人工抽检方案。
5. `lambda` 校准集、预注册候选值、两层 bootstrap 与功效/精度表。

## 3. 可修订项与不改变研究问题的边界

上述修改只是在不改变 Research Brief 的 RQ、H0/H1、主指标方向和五条件矩阵的前提下消除泄漏与不公平比较。若数据不足、判定器不可得或功效达不到预设精度，需要新增 `QUESTIONS.md` 问题并由用户决定降级路径；不能静默改变数据、主指标或效用门槛。

## 4. 审查结论

当前 protocol **未通过**。待 P-01～P-06 有证据地解决并完成复核后，再提交 G3；在此之前不写正式训练/评测代码。

## 5. G2 后闭环状态与新增问题

| 编号 | 复核证据 / 修订 | 状态 |
|---|---|---|
| P-01 | `设计核算/advbench_inventory.json`：固定 commit 的 520 条无法满足至少660条互斥分配，缺140条；原始CSV可校验SHA | 确认阻塞，需改划分或数据源，未获批 |
| P-02 | protocol §4冻结 A 方向、分离开发/测试；R-014/R-038卡明确原方法与项目简化版区别 | 文案修正；未实现、未验证 |
| P-03 | 替换5%样本不保证token相等，已撤回此保证；需记录input/监督token、步数和成本 | 仍待设计，不能记为已证明公平 |
| P-04 | 区分拒绝/行为遵从/内容安全；R-053与R-054证明判定器目标不同 | 概念更正；最终judge、解码、人工方案仍待G3 |
| P-05 | 随机方向使用各自m与尺度；不得用test挑lambda；新文献覆盖方向惩罚 | 文案修正；新范围和校准方案待审批 |
| P-06 | 解析情景表已算；prompt×seed交叉重采样；TOST复合p进入Holm | 部分完成；无seed方差实测，统计最终定义待定 |

新增高严重度 N-01：R-055 ProCon和R-056 SDP直接覆盖原C-1新颖性主张，R-057还覆盖输出路由解释。见 `01_选题/G1_重审建议.md`，必须先重审研究范围。

新增高严重度 N-02：将双判定器一致子集当主分母会产生选择偏差，建议全量主分母+分歧敏感性分析；Q-008历史默认已加待修订标记，未把新的降级方案静默视为已批准。

新增 N-03：原 H0/H1 不互补且有阈值空档，已禁止“未支持H1→证明H0→机制不存在”的推断；新的支持/排除/不确定决策规则待范围重审后明确。

独立复核：另一协作代理用公式逐行复算 `precision_scenarios.json`，并校验CSV条数/唯一值/SHA和660−520=140；均一致。复核同时发现protocol简写漏掉H1-1的关联符号，现已恢复原Brief的方向性（Δ=post−pre时β<0）。这些是设计/文档验证，不是GPU实验结果。
