# Protocol v0.3 方法论审查与G3出口

已完成：P-A01～P-A11逐项修订，外部来源和CPU可行性证据已落盘。
关键结论：设计可提交G3，但定位为有限条件估计，保留语义筛查、模板构念和测量限制。
需要用户决策：Q-012/G3，接受明确边界后实现和低成本冒烟；不是实验已验证。

> 2026-09-08。主代理自审，不冒称独立多代理审稿；依据16篇原文、官方来源与实际设计文件。旧审查保存于历史/。

| 编号 | 原严重度/问题 | v0.3修订与可核对证据 | 当前状态与保留限制 |
|---|---|---|---|
| P-A01 | 高：数据量/近重复/泄漏 | 固定官方HB159 test/41 val；Adv原520经规则与有限语义复核保留67/36/38/192；测试351。safety_split_g3.json含全部ID/hash/剔除；用途间保护性cluster互斥断言通过 | 设计闭环；不声称穷尽语义去重/语义OOD。较小方向池与保守分组导致覆盖变化，需用户在G3接受 |
| P-A02 | 高：SDP实现/层/token未知 | 官方摘要仍v1，无作者仓库链接；明确论文思想重建；L15–19映射block14–18，block output；完整序列末换行198；层/样本平均公式唯一。官方Qwen/PEFT源码与CPU token fixture支持可实现性 | 设计闭环；不称作者代码或数值复现。hook数值/adapter恢复留G3后冒烟，不伪造运行通过 |
| P-A03 | 高：判定器/非法标签/分母 | 主13b-cls与独立7b-val-cls revision、专用模板、解码、judge tokenizer截512均锁定；strict yes/no；固定全351分母；1次重试后未解缺失即暂停主结论 | 来源闭环；judge实际运行/系统误差尚未验证；不把无效标签当no，不筛judge一致子集 |
| P-A04 | 高：E等token不可执行 | 实际10k含1,354,795监督token；E逐更新配额67,739安全token、总量相同、625步、不空labels；3seed完整表CPU断言通过。输入额外约9.5%，固定模板循环67prompt | token可行性闭环；E并非等良性监督量/等输入或GPU成本；suffix masking局限明示，梯度归一留冒烟 |
| P-A05 | 高：初始化0/0 | 32步独立B预跑，8个固定训练侧样本（microbatch1）；唯一到64步重试；非有限/微小梯度停止，不重抽方向 | 设计闭环；非零/finite数值检查未运行；失败时停止整项校准，不挑能校准的bank |
| P-A06 | 高：同lambda/同norm不等强度 | 逐bank按非零点中位梯度比校准；C/D同batch/reference/rank/layer；记录固定checkpoint梯度/夹角/裁剪，禁止在线调参 | 设计闭环；局部、裁剪前范数匹配不等全轨迹或Adam更新匹配，不能作无混淆因果保证 |
| P-A07 | 高：3seed/3bank总体p/CI不稳 | 撤回三因子bootstrap近似p和Holm确认性判定；固定模型的效应估计、9配对值、3seed表、条件提示构成区间；不将C复制扩大样本 | 推断范围闭环；本MVE不对训练总体正式拒绝H0；总体确认需要另加独立重复和预算。此取舍明确提交G3 |
| P-A08 | 中：恢复/无损/多重性 | ±5pt/1pt保留工程目标，展示点差/条件区间；不称统计TOST/非劣；各任务/来源完整报告，E描述性 | 设计闭环；未达到目标不等H0真，达到固定目标不等总体等价 |
| P-A09 | 中：无退化后择配方 | 最终step625固定；旧d及50%方向消融线撤出主门槛；B−A10pt仅开发提示；不足则真实汇报，不自动加epoch/换数据 | 设计闭环；若G4认为信息收益不足由用户判断，当前不按潜在结果改题 |
| P-A10 | 中：训练数量/算力/存储 | 18训练公式、实际token、E输入成本、LoRA参数量有依据；100h含历史，≤10h冒烟；50GB磁盘分阶段模型缓存，G4前实测吞吐/显存/磁盘/剩余额度 | 规划闭环；真实GPU费用与兼容性未验证，不把情景当资源承诺 |
| P-A11 | 中：过度拒绝/人工一致性未知 | XSTest原作者CSV确认250无害；官方字符串规则明确降为探索性拒绝前缀代理；100条盲抽检具体分层，Q-008保留；不声称无过度拒绝或人—人κ | G3不阻塞；真正过度拒绝/人评有效性结论仍需实际标注，不用文案替代数据 |

## 本次实际完成的核验

- 新增4篇完整方法/实验/附录阅读及精读卡；本批累计16篇，不超授权。检索记录14条主题返回/14个唯一ID；交叉库失败如实记录。
- Qwen/data/judge官方commit和tokenizer资产已读取；主/开发模板从来源原样抽出；无模型权重下载或执行。
- CPU数据清单与E配额真实生成；每更新标签预算、用途间cluster互斥及字段/hash可重新核对。没有称其为模型实验。
- 最终代码核对发现拒绝词缩写正则遗漏，修正后按原hash规则替换58行并重算全部训练token/E配额；新清单该缩写匹配残留0，安全351划分hash不变。E-011与CPU更正记录保留前后证据；这项筛查修复不能升级为人工良性认证。
- 数值/公式歧义回看PDF；CWAC Eq11平方已确认；HARC归一化五级分与GPT IFEval的口径限制已进卡；官方HB159/41更正已登记。

## G3后的技术验收（现在未执行）

1. 依赖隔离安装与显存/磁盘/可用预算检查；base冻结、LoRA参数数量、初始化一致。
2. tokenizer与本次CPU fixture逐token一致，所有输入+生成预算不超模型上下文；hook输出与对应hidden state一致。
3. reference无梯度且adapter恢复；未训练时投影差/约束梯度为零，预跑后有限非零；三bank全部校准。
4. 按一次有效batch与16次microbatch分别核对CE/惩罚梯度归一；E配额和mask无错；没有梯度累积二次缩放。
5. 目标解码/judge模板/yes-no非法输出/完整分母与失败界的实现检查；用人工构造的无敏感任务fixtures验证解析，不用真实测试效果调参数。
6. 吞吐/峰值显存/磁盘/实际花费写实验日志，按18次总量重新核算后提交G4；不能拆成小批规避总项目阈值。

## 出口判定

**可提交G3；尚未批准G3，尚未实施实验。** 高严重度设计问题以真实来源、CPU核算或明确缩限结论处理；并非每项科学不确定性已消失。若用户要求总体显著性、穷尽语义无泄漏或已验证过度拒绝，本MVE不足，应增加证据而非修改文字冒充通过。

下一关卡材料见`G3_审批包.md`。研究范围D-006已获批，无需再次G1；本批G2已执行完成，无需重复批准现有来源。
