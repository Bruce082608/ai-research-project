# DECISIONS

> 所有影响方向的决定，含备选方案与决策人。条目编号 D-001 起。
> 本轮由 Fable 自主采用、无需用户决策的默认项（记录备查，不占 D-编号）：
> - 研究领域：大语言模型（第 0 节默认值）
> - 任务区间：选题 → 可投稿的论文初稿（第 0 节默认值）
> - 工作目录：`c:\Users\Ga\Desktop\AI科研项目`（当前工作区；目录结构按提示词 3.1 节建议创建）

---

## D-001 2026-09-03 边界条件定型（第 0 节占位符）
- 背景：首轮启动时第 0 节全部占位符为空，整理为 `QUESTIONS.md` Q-001～Q-005；用户于 2026-09-03 逐条回答。
- 备选方案：见 `QUESTIONS.md` 各条的选项列表。
- 决定：
  1. **方向偏好**（Q-001）：大语言模型，偏好「AI/LLM 算法相关」——候选课题以方法/算法贡献或算法性分析为主，不做纯系统工程（kernel、serving）或纯数据集/榜单发布类课题；由 Phase 1 生成 6 个候选后再收敛。
  2. **算力预算**（Q-002）：**暂未确定**。本机不进入实验阶段；显卡由用户后续解决。选题阶段的「算力可行性」评估采用以下**工作假设**（Fable 提出，非预算承诺，待 Q-006 确认）：MVE 应能在单节点（≤8×A100-80GB 级）合计 ≤200 GPU·h 或等价 API 花费内完成；full 版本 ≤ 5× MVE。**大算力阈值：未设**——任何实际算力或费用一律经 G4。
  3. **目标投稿**（Q-003）：ACL 系（ACL/EMNLP）+ ICLR 双轨兼容；每个候选标注更适合哪一类；进入 M5 前收敛到一个。
  4. **时间线**（Q-004）：4 周，2026-09-03 → 2026-10-01。里程碑预计日期写入 `PLAN.md`。时间不足时按第 8 节优先级：可复现的主结果 + 诚实的 Limitation > 消融完整性 > 写作润色。对 M2 的直接含义：精读篇数控制在 8–12 篇。
  5. **文献工具授权**（Q-005）：Phase 1 内一次性授权——可通过 arXiv API 核验候选课题与对抗审查中提到的文献的存在性与元数据（标题、作者、年份、arXiv 编号），不做扩展检索、不读摘要与正文；核验通过者记为「待核验（仅元数据）」。M2 起每次检索逐次 G2。
- 决策人：用户回答 Q-001～Q-005；第 2 条中的工作假设为 Fable 建议，待用户在 Q-006 批准。
- 影响的文件与步骤：`STATE.md`（参数回填）、`PLAN.md`（参数回填 + 里程碑日期）、S1.1.1 完成；`01_选题/候选课题.md` 中算力估算的口径与「更适合 ACL 系 / ICLR」标注。

## D-002 2026-09-03 算力形态定型（Q-006 的 A 部分）
- 背景：用户在 Q-002 中将显卡问题延后；2026-09-03 用户自行在 AutoDL 租用按量计费单卡实例并提供 SSH 入口，要求 Fable 完成后续配置。
- 备选方案：Fable 曾比较 AutoDL（国内、按量 ¥6/h 级 A100-80G）/ 阿里云 PAI、腾讯云 TI / RunPod、Vast.ai、Lambda（海外）；用户选择 AutoDL。
- 决定：
  1. **算力形态**：远程 GPU（Q-002 选项 B），**1× A800 80GB PCIe**（A100-80GB 的合规等价型号，显存相同、bf16 可用），AutoDL 按量计费，数据盘 50 GB。
  2. **总量**：用户口头预计 **约 100 GPU·h**。这低于 D-001 中「MVE ≤200 GPU·h」的工作假设，因此 **M3 protocol 的 MVE 预算上限按 100 GPU·h 撰写**，full 版本若超出则必须写明降级路径或另行申请。
  3. **大算力阈值**：用户尚未表态。按 Q-006 中 Fable 的推荐暂取 **25 GPU·h（总量的 1/4）**：单次任务预计 ≥25 GPU·h 或预计费用 ≥¥150 时先过 G4；冒烟测试与 <25 GPU·h 的单次运行不必。此为默认值，用户可随时改动。
  4. **环境与工作流**：远程环境基线见 `实验日志.md`；本地 ↔ 远程同步用 `03_实验/remote/sync.ps1`（push / pull / status / shell）；长任务用 `run_bg.sh`（tmux + 日志 + 可选自动关机）；GitHub 在该机房不可达，代码只经 scp 上传；HF 走 hf-mirror。
- 决策人：形态与总量为用户决定；阈值与 protocol 上限口径为 Fable 按 Q-006 默认处理，待用户确认。
- 影响的文件与步骤：`QUESTIONS.md` Q-006 标记「部分已答」；`PLAN.md` M4 的「依赖算力就位」条件已满足；M3 protocol 预算表按 100 GPU·h 撰写；`01_选题/候选课题.md` 中各候选的 MVE 预计算力若 >100 GPU·h 需在对抗审查的「算力可行性」列扣分。

## D-003 2026-09-03 选题收敛程序：把候选选择并入 G1（程序性决定）
- 背景：`PLAN.md` S1.2.3 原定在对抗审查后写 Q-007 并停下等用户选定，再写 Research Brief。用户在提供算力时指示「继续接下来的步骤，火力全开执行任务」。
- 备选方案：（A）按原计划停下等 Q-007；（B）对抗审查收敛后直接为排名第一的存活候选写 Research Brief，与候选选择一并提交 G1，用户在 G1 一次决定。
- 决定：B。理由：G1 本身就是「是否采用该课题与假设」的关卡，选择权仍完整保留在用户；提前写 Brief 只是让用户在决策时手里有完整材料，若用户选另一存活候选则重写 Brief，成本可控。研究问题的实质性决定没有被 Fable 代替。
- 决策人：Fable 建议（依用户「火力全开」指示），待用户在 G1 批准或否决。
- 影响的文件与步骤：`PLAN.md` S1.2.3 改为「写 Q-007 记录 G1 决策项」；`01_选题/对抗审查.md` 第 4 节「程序说明」；`01_选题/Research_Brief.md` 为候选 3 撰写。
- 附带记录：用户在聊天中提供的远程实例密码**未写入任何文件**；SSH 已为公钥免密（D-002），建议用户重置该密码。
